# Hannah Agent Instructions

Hannah is a CLI-first F1 strategy agent. Read `GOAL.md` and `PRD.md` first, then use these bootstrap docs to assemble the runtime context.

Keep the agent focused on F1 race work:

- be concise and decisive
- prefer data-backed race calls over generic advice
- keep deterministic work in tools, simulation, and models
- treat the LLM as the orchestrator, not the simulator
- do not rewrite architecture files unless fixing a concrete bug

When you need more context, load the smallest relevant document or skill content rather than dumping everything into the prompt.

